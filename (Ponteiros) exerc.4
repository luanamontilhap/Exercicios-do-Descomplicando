#include <stdio.h>

void troca(int *num1, int *num2){
  int *p, *m, temp;
  temp = *num1;
  *num1= *num2;
  *num2 = temp;
  
}


int main(void) {
  int num1, num2;

  printf ("Digite dois números: ");
  scanf ("%d %d", &num1, &num2);

   troca( &num1, &num2);
  printf ("Num 1: %d, Num2: %d", num1, num2);

  return 0;
}
